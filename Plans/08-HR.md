# خطة الوحدة: الموظفين (HR)

> **الهدف:** بناء نظام موارد بشرية متكامل يشمل سجلات الموظفين، الحضور، الرواتب، ونهاية الخدمة.

---

## 1. نظرة عامة

| البند | التفاصيل |
|-------|---------|
| **الاسم** | HR |
| **المجلد** | `src/modules/hr/` |
| **التبعيات** | Core, Auth, Settings, Accounting |
| **المدة** | أسبوعان (المرحلة 7) |
| **الأولوية** | متوسطة |

---

## 2. المتطلبات الوظيفية

### 2.1 سجلات الموظفين
- إضافة/تعديل/حذف موظف.
- بيانات شخصية (اسم، هوية، عنوان...).
- بيانات وظيفية (قسم، درجة، تاريخ التعيين...).
- المستندات (صورة الهوية، المؤهلات...).

### 2.2 الحضور والانصراف
- تسجيل الدخول/الخروج.
- أنواع الإجازات (سنوية، مرضية، طارئة...).
- ساعات العمل الإضافية.
- تقارير الحضور.

### 2.3 مسير الرواتب
- إعداد مسير الرواتب الشهري.
- الاستقطاعات (التأمين، الضرائب...).
- البدلات (السكن، النقل...).
- إصدار كشف راتب.

### 2.4 نهاية الخدمة
- حساب مستحقات نهاية الخدمة.
- مكافأة نهاية الخدمة.
- إجازات مستحقة.

### 2.5 التقارير
- مسير الرواتب.
- تقرير الحضور.
- رصيد الإجازات.
- نهاية الخدمة.
- تحليل الرواتب.

---

## 3. هيكل الملفات

```
src/modules/hr/
├── index.ts
├── types.ts
│
├── api/
│   ├── index.ts
│   ├── queries.ts
│   └── mutations.ts
│
├── components/
│   ├── HrPage.tsx
│   ├── EmployeesList.tsx
│   ├── EmployeeForm.tsx
│   ├── AttendancePage.tsx
│   ├── PayrollPage.tsx
│   ├── PayrollForm.tsx
│   └── index.ts
│
├── hooks/
│   ├── useEmployees.ts
│   ├── useAttendance.ts
│   ├── usePayroll.ts
│   └── index.ts
│
├── reports/
│   ├── PayrollSheet.tsx
│   ├── AttendanceReport.tsx
│   ├── LeaveBalance.tsx
│   ├── EndOfService.tsx
│   ├── SalaryAnalysis.tsx
│   └── index.ts
│
└── utils/
    ├── payrollCalculations.ts
    └── eosCalculations.ts
```

---

## 4. قاعدة البيانات (Schema)

```ts
// core/database/schema/hr.ts

// الموظفين
export const employees = pgTable('employees', {
  id: uuid('id').defaultRandom().primaryKey(),
  companyId: uuid('company_id').notNull(),
  employeeNumber: varchar('employee_number', { length: 50 }).notNull(),
  fullName: varchar('full_name', { length: 255 }).notNull(),
  nationalId: varchar('national_id', { length: 50 }),
  phone: varchar('phone', { length: 50 }),
  email: varchar('email', { length: 255 }),
  address: text('address'),
  departmentId: uuid('department_id'),
  position: varchar('position', { length: 100 }),
  grade: varchar('grade', { length: 50 }),
  hireDate: date('hire_date').notNull(),
  terminationDate: date('termination_date'),
  baseSalary: numeric('base_salary', { precision: 18, scale: 4 }).notNull(),
  isActive: boolean('is_active').default(true).notNull(),
  createdAt: timestamp('created_at', { withTimezone: true }).defaultNow(),
  updatedAt: timestamp('updated_at', { withTimezone: true }).defaultNow(),
});

// الأقسام
export const departments = pgTable('departments', {
  id: uuid('id').defaultRandom().primaryKey(),
  companyId: uuid('company_id').notNull(),
  name: varchar('name', { length: 100 }).notNull(),
  managerId: uuid('manager_id'),
  createdAt: timestamp('created_at', { withTimezone: true }).defaultNow(),
});

// الحضور
export const attendance = pgTable('attendance', {
  id: uuid('id').defaultRandom().primaryKey(),
  companyId: uuid('company_id').notNull(),
  employeeId: uuid('employee_id').references(() => employees.id, { onDelete: 'cascade' }).notNull(),
  date: date('date').notNull(),
  checkIn: timestamp('check_in').notNull(),
  checkOut: timestamp('check_out'),
  overtimeHours: numeric('overtime_hours', { precision: 5, scale: 2 }).default('0'),
  notes: text('notes'),
  createdAt: timestamp('created_at', { withTimezone: true }).defaultNow(),
});

// الإجازات
export const leaves = pgTable('leaves', {
  id: uuid('id').defaultRandom().primaryKey(),
  companyId: uuid('company_id').notNull(),
  employeeId: uuid('employee_id').notNull(),
  type: varchar('type', { length: 50 }).notNull(), // annual, sick, emergency
  startDate: date('start_date').notNull(),
  endDate: date('end_date').notNull(),
  days: numeric('days', { precision: 5, scale: 2 }).notNull(),
  status: varchar('status', { length: 20 }).default('pending').notNull(), // pending, approved, rejected
  createdAt: timestamp('created_at', { withTimezone: true }).defaultNow(),
});

// مسير الرواتب
export const payrollRuns = pgTable('payroll_runs', {
  id: uuid('id').defaultRandom().primaryKey(),
  companyId: uuid('company_id').notNull(),
  month: integer('month').notNull(),
  year: integer('year').notNull(),
  totalAmount: numeric('total_amount', { precision: 18, scale: 4 }).notNull(),
  status: varchar('status', { length: 20 }).default('draft').notNull(),
  createdAt: timestamp('created_at', { withTimezone: true }).defaultNow(),
});

// بنود مسير الرواتب
export const payrollLines = pgTable('payroll_lines', {
  id: uuid('id').defaultRandom().primaryKey(),
  payrollRunId: uuid('payroll_run_id').references(() => payrollRuns.id, { onDelete: 'cascade' }).notNull(),
  employeeId: uuid('employee_id').notNull(),
  baseSalary: numeric('base_salary', { precision: 18, scale: 4 }).notNull(),
  allowances: numeric('allowances', { precision: 18, scale: 4 }).default('0'),
  deductions: numeric('deductions', { precision: 18, scale: 4 }).default('0'),
  overtime: numeric('overtime', { precision: 18, scale: 4 }).default('0'),
  netSalary: numeric('net_salary', { precision: 18, scale: 4 }).notNull(),
  createdAt: timestamp('created_at', { withTimezone: true }).defaultNow(),
});
```

---

## 5. تقسيم المهام

### الأسبوع الأول

| # | المهمة | المدة | الأولوية |
|---|--------|-------|---------|
| 1 | Schema (employees, departments) | 4 ساعات | عالية |
| 2 | قائمة الموظفين | 4 ساعات | عالية |
| 3 | نموذج الموظف | 6 ساعات | عالية |
| 4 | API للموظفين | 4 ساعات | عالية |
| 5 | الحضور والانصراف | 6 ساعات | عالية |

### الأسبوع الثاني

| # | المهمة | المدة | الأولوية |
|---|--------|-------|---------|
| 6 | مسير الرواتب | 8 ساعات | عالية |
| 7 | نهاية الخدمة | 4 ساعات | متوسطة |
| 8 | التقارير (5 تقارير) | 8 ساعات | متوسطة |
| 9 | اختبارات الوحدات | 4 ساعات | متوسطة |

---

## 6. معايير القبول

- [ ] إضافة موظف جديد تعمل.
- [ ] تعديل بيانات موظف تعمل.
- [ ] تسجيل الحضور يعمل.
- [ ] إنشاء مسير رواتب يعمل.
- [ ] حساب صافي الراتب صحيح.
- [ ] نهاية الخدمة تحسب بشكل صحيح.
- [ ] التقارير تعرض بيانات صحيحة.
- [ ] اختبارات الوحدات تمر.

---

## 7. المخرجات النهائية

1. **نظام سجلات الموظفين**.
2. **الحضور والانصراف**.
3. **مسير الرواتب**.
4. **نهاية الخدمة**.
5. **5 تقارير** متقدمة.
6. **API كامل** للموارد البشرية.
7. **اختبارات** شاملة.

---

*تاريخ الإنشاء: 2026-05-24 | المسؤول: HR Module Lead*
